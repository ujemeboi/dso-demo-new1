apiVersion: v1
kind: Pod
metadata:
  labels:
    app: spring-build-ci
spec:
  containers:
    - name: maven
      [cite_start]image: maven:3.9-eclipse-temurin-17
      command: ["cat"]
      tty: true
      volumeMounts:
        - name: m2
          mountPath: /root/.m2/
    - name: docker-tools
      [cite_start]image: docker:dind
      command: ["cat"]
      tty: true
      volumeMounts:
        - mountPath: /var/run
          name: docker-sock
    - name: trufflehog
      [cite_start]image: trufflesecurity/trufflehog:latest
      command: ["cat"]
      tty: true
    - name: licensefinder
      image: licensefinder/license_finder:latest
      command: ["cat"]
      tty: true
  volumes:
    - name: m2
      hostPath:
        path: /tmp/.m2/
    - name: docker-sock
      hostPath:
        path: /var/run
    - name: trivycache
      hostPath:
        path: /tmp/trivycache/
